FROM node:20-alpine

WORKDIR /app

COPY backend/package*.json ./
RUN npm ci --only=production

COPY backend/dist ./dist
COPY database/schema.prisma ./

RUN npm install -g prisma

EXPOSE 3000

CMD ["node", "dist/server.js"]
